# ScanHead Core

Модульное ядро для системы инвентаризации и управления складом.

## 🎯 Цель

Разделение монолитной логики legacy-проектов (StickerMaker, ScanHead) на независимые, тестируемые модули с четкими интерфейсами. Сборка нового приложения `scanhead-combine` на основе этих модулей с соблюдением принципов SOLID, Law of Demeter и слоистой архитектуры.

## 📦 Структура репозитория

```text
├── libs/                   # Библиотеки (модули)
│   ├── core/               # Инфраструктура: bootstrap, инициализация
│   ├── i18n/               # Интернационализация и сообщения
│   ├── domain/             # Доменная логика: калькулятор расхождений
│   ├── repository/         # Слой доступа к данным (абстракции + SQLite)
│   ├── scanner_input/      # Обработка ввода сканеров (HID, камера)
│   ├── printing/           # Генерация этикеток и печатных форм
│   ├── config/             # Управление пресетами и настройками
│   ├── utils/              # Утилиты: поиск, генерация имен файлов
│   ├── parsers/            # Парсеры внешних данных (Excel, CSV, XML)
│   ├── inventory_parser/   # Парсер файлов инвентаризации
│   ├── command_handler/    # Обработка текстовых команд сканера
│   ├── domain_models/      # Dataclass-модели предметной области
│   ├── data_exporters/     # Экспорт в Excel, CSV
│   ├── ui_components/      # Переиспользуемые UI-виджеты (Accordion и др.)
│   │
│   # Базовые модули
│   ├── config_manager/     # Управление конфигурацией (JSON, слияние конфигов)
│   ├── path_utils/         # Утилиты работы с путями (OS-independent)
│   ├── article_normalizer/ # Нормализация артикулов (очистка, форматирование)
│   ├── barcode_validator/  # Валидация контрольных сумм (EAN-13, Code-128)
│   ├── barcode_generator/  # Генерация изображений штрих-кодов и QR
│   ├── db_manager/         # Абстракция БД (SQLite, Postgres, CSV, XML, XLSX)
│   ├── data_validator/     # Сквозная валидация бизнес-объектов
│   └── data_enricher/      # Обогащение данных (подстановка названий, тегов)
│
├── gui/                    # GUI приложение на customtkinter + ttk
│   ├── main_window.py      # Корневое окно, DI-контейнер, табы
│   ├── search_bar.py       # Поиск с debounce 300мс + автодополнение
│   ├── product_details.py  # Детали товара + кнопки редактирования
│   ├── print_queue.py      # Treeview очереди + toolbar + меню колонок
│   ├── sticker_preview.py  # Превью стикера + noimage.png
│   ├── tabs/               # Вкладки (search_address_tab, inventory_tab)
│   ├── dialogs/            # Диалоги (settings, field_editor, sticker_editor)
│   ├── services/           # Сервисы GUI (interfaces, adapters, stubs)
│   ├── widgets/            # Кастомные виджеты
│   └── windows/            # Дочерние окна (Toplevel)
├── services/               # Сервисный слой (бизнес-логика)
├── config/                 # Конфигурационные файлы
├── data/                   # Данные и ресурсы
└── main.py                 # Точка входа приложения
```

## 🚀 Статус разработки

### Активные ветки
- `main` — стабильная версия
- `gui-framework-dev` — разработка GUI на tkinter (текущая)

### Модули (все готовы к использованию)

| Модуль | Версия | Статус | Назначение |
|--------|--------|--------|------------|
| `core/bootstrap` | 0.1.0 | ✅ | Инициализация логов, исключений, окружения |
| `i18n/messages` | 0.1.0 | ✅ | Сообщения на RU/EN, локализация |
| `domain/discrepancy` | 0.1.0 | ✅ | Калькулятор расхождений план/факт |
| `repository` | 0.1.0 | ✅ | Абстрактные репозитории + SQLite реализация |
| `scanner_input` | 0.1.0 | ✅ | HID-сканер, заглушка камеры, буферизация |
| `utils/fuzzy_search` | 0.1.0 | ✅ | Нечеткий поиск и фильтрация |
| `utils/file_naming` | 0.1.0 | ✅ | Генератор имен файлов по шаблонам |
| `printing/sticker_generator` | 0.1.0 | ✅ | Генерация этикеток (Code128, EAN13, QR) |
| `config/preset_manager` | 0.1.0 | ✅ | Сохранение/загрузка пресетов JSON |
| `inventory_parser` | 0.1.0 | ✅ | Парсер Excel-файлов инвентаризации |
| `command_handler` | 0.2.0 | ✅ | Обработка команд сканера (RU/EN раскладки) |
| `domain_models` | 0.1.0 | ✅ | Dataclass-модели (Product, CountItem, InventoryItem) |
| `data_exporters` | 0.1.0 | ✅ | Экспорт в Excel и CSV |
| `ui_components` | 0.1.0 | ✅ | UI-виджеты для tkinter |
| `config_manager` | 0.1.0 | ✅ | Управление JSON-конфигурацией |
| `path_utils` | 0.1.0 | ✅ | Работа с путями (PyInstaller-compatible) |
| `article_normalizer` | 0.1.0 | ✅ | Нормализация артикулов |
| `barcode_validator` | 0.1.0 | ✅ | Валидация EAN-13, Code-128 |
| `barcode_generator` | 0.1.0 | ✅ | Генерация штрих-кодов и QR |
| `db_manager` | 0.1.0 | 🏗️ | Абстракция БД (SQLite готов, другие в разработке) |
| `data_validator` | 0.1.0 | ✅ | Конвейер валидации данных |
| `data_enricher` | 0.1.0 | ✅ | Преобразование сырых данных в бизнес-объекты |
| `gui/framework` | 0.2.0 | 🚧 | GUI-каркас (customtkinter, DI, моки) |

## 🛡️ Правила разработки

### Ветвление
- `main` — стабильная версия, только через Pull Requests
- `dev/*` — ветки для разработки новых фич
- `fix/*` — ветки для исправления багов

### Модульность
- Каждый модуль в `libs/` должен быть независимым
- Запрещены циклические импорты между модулями
- Бизнес-логика не зависит от GUI

### Коммиты
- Префиксы: `feat:`, `fix:`, `refactor:`, `docs:`, `chore:`
- Понятные сообщения о изменениях

## 📚 Документация

Каждый модуль в `libs/` содержит собственный `README.md` с примерами использования.

## 🔜 Ближайшие задачи

1. Завершение разработки GUI на tkinter (ветка `gui-framework-dev`)
2. Реализация драйверов для `db_manager` (XML, XLSX)
3. Интеграционные тесты для модулей генерации и печати
4. Парсеры накладных (`parsers/invoice_parser`)
5. Поддержка камеры как источника штрих-кодов