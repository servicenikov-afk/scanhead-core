# ScanHead Core / Module Extractor

Репозиторий содержит ядро модульной архитектуры для проекта ScanHead и утилиты, выделенные из legacy-проектов (StickerMaker, ScanHead).

## 🎯 Цель
Разделение монолитной логики старых проектов на независимые, тестируемые модули с четкими интерфейсами. Сборка нового приложения `scanhead-combine` на основе этих модулей с соблюдением принципов SOLID, Law of Demeter и слоистой архитектуры.

## 📦 Структура репозитория

```text
├── libs/                   # Библиотеки (модули)
│   ├── config_manager/     # Управление конфигурацией (JSON, слияние конфигов)
│   ├── path_utils/         # Утилиты работы с путями (OS-independent)
│   ├── article_normalizer/ # Нормализация артикулов (очистка, форматирование)
│   ├── barcode_checker/    # [Legacy] Старая проверка штрих-кодов (на удаление/рефакторинг)
│   ├── barcode_validator/  # Валидация контрольных сумм (EAN-13, Code-128)
│   ├── barcode_generator/  # Генерация изображений штрих-кодов и QR (исправлен баг со слэшами)
│   ├── db_manager/         # Абстракция БД (SQLite, Postgres, CSV, XML, XLSX) + Миграции
│   ├── data_validator/     # Сквозная валидация бизнес-объектов
│   └── data_enricher/      # Обогащение данных (подстановка названий, тегов)
├── projects/               # Исходные legacy-проекты (только для чтения, источник логики)
│   ├── project_a/          # StickerMaker
│   └── project_b/          # ScanHead
└── scanhead-combine/       # [В разработке] Новый проект, собирающий модули вместе

🚀 Статус разработки
Модуль
Версия
Статус
Примечание
config_manager
0.1.0
✅ Готов
Базовая загрузка/сохранение JSON
path_utils
0.1.0
✅ Готов
Кроссплатформенные пути
article_normalizer
0.1.0
⚠️ Требует доработки
Нужна поддержка префиксов и валидации длины
barcode_checker
0.1.0
🗑️ Устарел
Заменен на barcode_validator и barcode_generator
barcode_validator
0.1.0
✅ Готов
Валидация контрольных сумм
barcode_generator
0.1.0
✅ Готов
Исправлен баг обрезания QR при наличии слэшей (/)
db_manager
0.1.0
🏗️ Каркас
Готова абстракция, нужны драйверы (XML, XLSX) и миграции
data_validator
0.1.0
✅ Готов
Конвейер валидации
data_enricher
0.1.0
✅ Готов
Преобразование сырых данных в объекты
🛡️ Правила разработки
Ветвление:
main — стабильная версия, проходит только через Pull Requests.
dev/* — ветки для разработки новых фич.
fix/* — ветки для исправления багов.
Модульность:
Каждый модуль в папке libs/ должен быть независимым.
Запрещены циклические импорты между модулями.
Интерфейсы модулей должны быть стабильными.
Тестирование:
Перед коммитом обязательно проверять, что модуль работает изолированно.
Особое внимание уделять краевым случаям (например, спецсимволы в данных для QR-кодов).
Коммиты:
Использовать префиксы: feat:, fix:, refactor:, docs:, chore:.
Сообщение должно быть понятным ("что сделано" и "зачем").
🔜 Ближайшие задачи
Реализовать драйверы для db_manager (XML, XLSX).
Написать интеграционные тесты для barcode_generator.
Начать сборку прототипа scanhead-combine.