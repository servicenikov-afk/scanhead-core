При фиксинге и ребилдинге пользуемся следующими инструкциями:
1)"программист" слабо знает программирование. Не путаем его, не даём избыточных советов, отвечаем лаконично, экономим время и токены.
2)код выдаём информативно: либо целиком листинг скрипта, либо, если изменения касаются одной функции, указываем путь/до/скрипта и начало объявления def _функции, потом полный текст изменённой функции. Если надо добавить 1-2 импорта или изменения переменных, указываем где и текст "было"-"стало" или "добавить импорт {место} (в начале кода/после объявления функции *def function* и т.п.), в этом случае полные листинги не выводим!
3)никаких отдельных тестов: либо сразу меняем код и проверяем, либо вносим диагностику для выявления багов и проверяем.
4)не выводим каскадные изменения одного и того же кода (сначала меняем функцию так, потом так, а потом добавляем это). Выводим сразу код с комплексом всех изменений.
5)уплотняем код: убираем избыточные комментарии, переносы, пробелы. Оптимально - 1 коментарий в начале кода с путём до скрипта (# gui/main_window.py).
6)сверяемся с этими инструкциями перед выдачей ответов каждый раз.

12:25 27.03.2026
По идее в инвентаризации активная (только что введёная) позиция во фрейме "Результаты инвентаризации" должна менять фон на оранжевый, но этого не происходит.
8:57 03.04.2026
При подгрузке ведомости в "Инвентаризацию", по факту в ведомости 110 позиций, программа в консоли пишет INFO:core.inventory_parser:Найдено позиций: 105. Подозреваю, что не подхватились позиции, где "Номенклатура.Артикул 2" пустая, а артикул есть только в "Артикул". Надо учитывать все позиции, где есть "Номенклатура.Артикул 2" ИЛИ "Артикул". Поиск и учёт, кстати, тоже надо производить по этим двум полям позиции. Ибо есть позиции, где заполнено только одно из этих полей или маркирована позиция на упаковке артикулом из "Артикул" вместо "Номенклатура.Артикул 2".
9:29 03.04.2026
Требуется вывод окна ошибки в "Инвентаризации" - "Позиция ["Артикул" "Номенклатура.Артикул 2" "Наименование"] отсутствует в ведомости! [OK]".
9:35 03.04.2026
Продублировать функцию ввода кода [CMD]FINISH_INV кнопкой "Завершить и сохранить" во вкладке "Инвентаризации".
9:38 03.04.2026
Изменить функционал сохранения отчёта по инвентаризации: запросить место и имя сохраняемого файла, скопировать оригинал файла ведомости, внести в скопированном файле в соответствующие позиции значения фактических остатков в колонку "Факт". Выделить ячейку цветом: зелёный - если сошлось, красным - если недосдача, желтым - если излишек.